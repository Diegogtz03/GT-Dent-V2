import styles from './PrescriptionModal.module.css';
import Modal from '../Modal/Modal';

function PrescriptionModal({ secondaryClassName, showModal }) {
  return (
    <Modal title={'Receta'}>

    </Modal>
  );
}

export default PrescriptionModal;